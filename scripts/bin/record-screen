#!/usr/bin/env bash

OUTPUT="/media/myfiles/Videos/Recordings/$(date +%Y-%m-%d_%H-%M).mp4"

gpu-screen-recorder -w screen -f 60 -o "$OUTPUT" -a "default_output|default_input" -s 1920x1080 -k h264 -q high
