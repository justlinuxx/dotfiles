windowrule {
  name = floating-window
  match:tag = floating-window

  float = on
  center = on
  size = 875 600
}

windowrule {
  name = add-floating-window-tag
  match:class = (mine.impala|mine.bluetui|mine.wiremix)

  tag = +floating-window
}
